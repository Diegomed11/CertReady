// Configuración de Cube.
//
// La conexión (ClickHouse) y el modo se toman del entorno (ver docker-compose.yml
// y .env.example). El modelo dimensional —cubos, medidas, dimensiones y
// pre-agregaciones— vive en model/.
module.exports = {};
